language: php

php:
  - 5.6

sudo: required

before_script:
  - curl -OL https://squizlabs.github.io/PHP_CodeSniffer/phpcs.phar
  - curl -OL https://squizlabs.github.io/PHP_CodeSniffer/phpcbf.phar
script:
  - php phpcs --standard=PSR2 --extensions=php -n src
